# Maintainer Notes

`main` is the stable portfolio/release branch. `development` is the active integration branch.

Keep the application behavior and documentation synchronized. Before a release, verify the main user flow, CI, database setup, and security checklist.
